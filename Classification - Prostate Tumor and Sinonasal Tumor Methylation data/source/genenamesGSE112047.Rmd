---
title: "Untitled"
author: "G.S.B"
date: "2023-07-02"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
# Step 1: Install ChAMP
# Step 1: Install required packages
install.packages("ChAMP")
install.packages("minfi")

# Step 2: Load the required packages
library(ChAMP)
library(minfi)
library(data.table)

```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}

beta<-fread("C:/Users/bless/OneDrive/MS_Studies/AML/GSE112047/Beta.csv")
pheno<-fread("C:/Users/bless/OneDrive/MS_Studies/AML/GSE112047/Pheno.csv")

beta<-t(data.frame(beta[,1:30000]))
col<-as.character(beta[1, ])
beta<-beta[-1,]
colnames(beta)<-col
pheno<-data.frame(pheno[2:nrow(pheno),])
betaaa<-apply(beta, 2, as.numeric)
betaa<-data.frame((beta))
#betaa<-data.matrix(as.numeric(beta))

for(i in 1: length(colnames(beta)))
{
  colnames(beta)[i]<-gsub("\\_", "", colnames(beta)[i])
  #group$sample[i]<-gsub("\\.", "-", group$sample[i])
  #print(colnames(mirna)[i])
}

for(i in 1: nrow(pheno))
{
  pheno$V1[i]<-gsub("\\_", "", pheno$V1[i])
  #group$sample[i]<-gsub("\\.", "-", group$sample[i])
  #print(colnames(mirna)[i])
}

for(i in 1: nrow(pheno))
{
  
  if(pheno$V2[i]=="Adjacent normal prostate tissue")
  {
    pheno$V2[i]=0
  }
  else
  {
    pheno$V2[i]=1
  }
  #group$sample[i]<-gsub("\\.", "-", group$sample[i])
  #print(colnames(mirna)[i])
}

group<-as.numeric(pheno$V2)
champ_obj <- champ.DMP(beta = betaaa, pheno = pheno$V2,adjPVal = 0.05)
champ_obj[["NumericVariable"]][["gene"]]

```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
